name: weaktr_refinery
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - pytorch>=2.0.0
  - torchvision>=0.15.0
  - pytorch-cuda=12.1  # Change to 12.1 for newer GPUs
  - numpy>=1.24.0
  - scipy>=1.10.0
  - pillow>=10.0.0
  - tqdm>=4.65.0
  - matplotlib>=3.7.0
  
  # Install via pip
  - pip
  - pip:
    - timm>=0.9.0